language: rust
branches:
  only:
  - master
matrix:
  include:
    - name: "Windows Build"
      os: windows
      rust: nightly
      env:
        - RUSTFLAGS="-C link-arg=/MDd"
      script:
        -  cargo rustc -- -Z print-link-args
